#include "PRJNAME_defs.h"
#include "PRJNAME_io.h"

int validate_opts(PRJNAME_options_t *opts)
{
    // if (!is_directory(opts->proj_tmpls_root_path)) {
    //   rprintf(opts, LOG_LEVEL_ERROR, "Template Root Path (%s) does not
    //   exist\n", opts->proj_tmpls_root_path); return -1;
    // }

    // if (opts->command == cmd_generate_project) {
    //   if (ez_matches(PNAME_PATTERN, opts->project_name) == EZREGEX_NOMATCH) {
    //     rprintf(opts, LOG_LEVEL_ERROR, "Project name must match "
    //     PNAME_PATTERN
    //     "\n"); return -1;
    //   }
    // }

    return 0;
}
